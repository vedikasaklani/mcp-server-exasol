# Base URL of server_management.api.api (the backend, port 8000 by default).
# When testing through ngrok (e.g. for a remote GitHub App webhook setup),
# this can stay pointed at localhost - ngrok is for inbound traffic to the
# backend, not for this dashboard's outbound calls to it.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
